
  # Xbot (Experimentals)

  This is a code bundle for Xbot (Experimentals). The original project is available at https://www.figma.com/design/ch2DRI522Ij0QmfBd1Cew2/Xbot--Experimentals-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  